/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getOptionDecoder,
  getOptionEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type Option,
  type OptionOrNullable,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { CODE_IN_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const SET_MERKLE_ROOT_DISCRIMINATOR = new Uint8Array([
  43, 24, 91, 60, 240, 137, 28, 102,
]);

export function getSetMerkleRootDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SET_MERKLE_ROOT_DISCRIMINATOR,
  );
}

export type SetMerkleRootInstruction<
  TProgram extends string = typeof CODE_IN_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountConfig extends string
        ? WritableAccount<TAccountConfig>
        : TAccountConfig,
      ...TRemainingAccounts,
    ]
  >;

export type SetMerkleRootInstructionData = {
  discriminator: ReadonlyUint8Array;
  newRoot: ReadonlyUint8Array;
  newAuthority: Option<Address>;
};

export type SetMerkleRootInstructionDataArgs = {
  newRoot: ReadonlyUint8Array;
  newAuthority: OptionOrNullable<Address>;
};

export function getSetMerkleRootInstructionDataEncoder(): Encoder<SetMerkleRootInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["newRoot", fixEncoderSize(getBytesEncoder(), 32)],
      ["newAuthority", getOptionEncoder(getAddressEncoder())],
    ]),
    (value) => ({ ...value, discriminator: SET_MERKLE_ROOT_DISCRIMINATOR }),
  );
}

export function getSetMerkleRootInstructionDataDecoder(): Decoder<SetMerkleRootInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["newRoot", fixDecoderSize(getBytesDecoder(), 32)],
    ["newAuthority", getOptionDecoder(getAddressDecoder())],
  ]);
}

export function getSetMerkleRootInstructionDataCodec(): Codec<
  SetMerkleRootInstructionDataArgs,
  SetMerkleRootInstructionData
> {
  return combineCodec(
    getSetMerkleRootInstructionDataEncoder(),
    getSetMerkleRootInstructionDataDecoder(),
  );
}

export type SetMerkleRootAsyncInput<
  TAccountAuthority extends string = string,
  TAccountConfig extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  config?: Address<TAccountConfig>;
  newRoot: SetMerkleRootInstructionDataArgs["newRoot"];
  newAuthority: SetMerkleRootInstructionDataArgs["newAuthority"];
};

export async function getSetMerkleRootInstructionAsync<
  TAccountAuthority extends string,
  TAccountConfig extends string,
  TProgramAddress extends Address = typeof CODE_IN_PROGRAM_ADDRESS,
>(
  input: SetMerkleRootAsyncInput<TAccountAuthority, TAccountConfig>,
  config?: { programAddress?: TProgramAddress },
): Promise<
  SetMerkleRootInstruction<TProgramAddress, TAccountAuthority, TAccountConfig>
> {
  // Program address.
  const programAddress = config?.programAddress ?? CODE_IN_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([99, 111, 110, 102, 105, 103])),
      ],
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.config),
    ],
    data: getSetMerkleRootInstructionDataEncoder().encode(
      args as SetMerkleRootInstructionDataArgs,
    ),
    programAddress,
  } as SetMerkleRootInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountConfig
  >);
}

export type SetMerkleRootInput<
  TAccountAuthority extends string = string,
  TAccountConfig extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  config: Address<TAccountConfig>;
  newRoot: SetMerkleRootInstructionDataArgs["newRoot"];
  newAuthority: SetMerkleRootInstructionDataArgs["newAuthority"];
};

export function getSetMerkleRootInstruction<
  TAccountAuthority extends string,
  TAccountConfig extends string,
  TProgramAddress extends Address = typeof CODE_IN_PROGRAM_ADDRESS,
>(
  input: SetMerkleRootInput<TAccountAuthority, TAccountConfig>,
  config?: { programAddress?: TProgramAddress },
): SetMerkleRootInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountConfig
> {
  // Program address.
  const programAddress = config?.programAddress ?? CODE_IN_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.config),
    ],
    data: getSetMerkleRootInstructionDataEncoder().encode(
      args as SetMerkleRootInstructionDataArgs,
    ),
    programAddress,
  } as SetMerkleRootInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountConfig
  >);
}

export type ParsedSetMerkleRootInstruction<
  TProgram extends string = typeof CODE_IN_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    config: TAccountMetas[1];
  };
  data: SetMerkleRootInstructionData;
};

export function parseSetMerkleRootInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSetMerkleRootInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { authority: getNextAccount(), config: getNextAccount() },
    data: getSetMerkleRootInstructionDataDecoder().decode(instruction.data),
  };
}
