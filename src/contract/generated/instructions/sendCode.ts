/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU8Decoder,
  getU8Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { CODE_IN_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const SEND_CODE_DISCRIMINATOR = new Uint8Array([
  239, 129, 35, 208, 62, 168, 110, 201,
]);

export function getSendCodeDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(SEND_CODE_DISCRIMINATOR);
}

export type SendCodeInstruction<
  TProgram extends string = typeof CODE_IN_PROGRAM_ADDRESS,
  TAccountUser extends string | AccountMeta<string> = string,
  TAccountCodeAccount extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountUser extends string
        ? WritableSignerAccount<TAccountUser> & AccountSignerMeta<TAccountUser>
        : TAccountUser,
      TAccountCodeAccount extends string
        ? WritableAccount<TAccountCodeAccount>
        : TAccountCodeAccount,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type SendCodeInstructionData = {
  discriminator: ReadonlyUint8Array;
  code: string;
  beforeTx: string;
  method: number;
  decodeBreak: number;
};

export type SendCodeInstructionDataArgs = {
  code: string;
  beforeTx: string;
  method: number;
  decodeBreak: number;
};

export function getSendCodeInstructionDataEncoder(): Encoder<SendCodeInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["code", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ["beforeTx", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ["method", getU8Encoder()],
      ["decodeBreak", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: SEND_CODE_DISCRIMINATOR }),
  );
}

export function getSendCodeInstructionDataDecoder(): Decoder<SendCodeInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["code", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ["beforeTx", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ["method", getU8Decoder()],
    ["decodeBreak", getU8Decoder()],
  ]);
}

export function getSendCodeInstructionDataCodec(): Codec<
  SendCodeInstructionDataArgs,
  SendCodeInstructionData
> {
  return combineCodec(
    getSendCodeInstructionDataEncoder(),
    getSendCodeInstructionDataDecoder(),
  );
}

export type SendCodeInput<
  TAccountUser extends string = string,
  TAccountCodeAccount extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  user: TransactionSigner<TAccountUser>;
  codeAccount: Address<TAccountCodeAccount>;
  systemProgram?: Address<TAccountSystemProgram>;
  code: SendCodeInstructionDataArgs["code"];
  beforeTx: SendCodeInstructionDataArgs["beforeTx"];
  method: SendCodeInstructionDataArgs["method"];
  decodeBreak: SendCodeInstructionDataArgs["decodeBreak"];
};

export function getSendCodeInstruction<
  TAccountUser extends string,
  TAccountCodeAccount extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof CODE_IN_PROGRAM_ADDRESS,
>(
  input: SendCodeInput<
    TAccountUser,
    TAccountCodeAccount,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): SendCodeInstruction<
  TProgramAddress,
  TAccountUser,
  TAccountCodeAccount,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? CODE_IN_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    user: { value: input.user ?? null, isWritable: true },
    codeAccount: { value: input.codeAccount ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.user),
      getAccountMeta(accounts.codeAccount),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getSendCodeInstructionDataEncoder().encode(
      args as SendCodeInstructionDataArgs,
    ),
    programAddress,
  } as SendCodeInstruction<
    TProgramAddress,
    TAccountUser,
    TAccountCodeAccount,
    TAccountSystemProgram
  >);
}

export type ParsedSendCodeInstruction<
  TProgram extends string = typeof CODE_IN_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    user: TAccountMetas[0];
    codeAccount: TAccountMetas[1];
    systemProgram: TAccountMetas[2];
  };
  data: SendCodeInstructionData;
};

export function parseSendCodeInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSendCodeInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      user: getNextAccount(),
      codeAccount: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getSendCodeInstructionDataDecoder().decode(instruction.data),
  };
}
