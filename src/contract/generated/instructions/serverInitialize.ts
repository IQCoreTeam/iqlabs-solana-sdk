/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { CODE_IN_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  expectSome,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const SERVER_INITIALIZE_DISCRIMINATOR = new Uint8Array([
  193, 244, 16, 234, 35, 101, 175, 99,
]);

export function getServerInitializeDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SERVER_INITIALIZE_DISCRIMINATOR,
  );
}

export type ServerInitializeInstruction<
  TProgram extends string = typeof CODE_IN_PROGRAM_ADDRESS,
  TAccountUser extends string | AccountMeta<string> = string,
  TAccountServerAccount extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountUser extends string
        ? WritableSignerAccount<TAccountUser> & AccountSignerMeta<TAccountUser>
        : TAccountUser,
      TAccountServerAccount extends string
        ? WritableAccount<TAccountServerAccount>
        : TAccountServerAccount,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type ServerInitializeInstructionData = {
  discriminator: ReadonlyUint8Array;
  serverId: string;
  serverType: string;
  allowedMerkleRoot: string;
};

export type ServerInitializeInstructionDataArgs = {
  serverId: string;
  serverType: string;
  allowedMerkleRoot: string;
};

export function getServerInitializeInstructionDataEncoder(): Encoder<ServerInitializeInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["serverId", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ["serverType", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      [
        "allowedMerkleRoot",
        addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder()),
      ],
    ]),
    (value) => ({ ...value, discriminator: SERVER_INITIALIZE_DISCRIMINATOR }),
  );
}

export function getServerInitializeInstructionDataDecoder(): Decoder<ServerInitializeInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["serverId", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ["serverType", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    [
      "allowedMerkleRoot",
      addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder()),
    ],
  ]);
}

export function getServerInitializeInstructionDataCodec(): Codec<
  ServerInitializeInstructionDataArgs,
  ServerInitializeInstructionData
> {
  return combineCodec(
    getServerInitializeInstructionDataEncoder(),
    getServerInitializeInstructionDataDecoder(),
  );
}

export type ServerInitializeAsyncInput<
  TAccountUser extends string = string,
  TAccountServerAccount extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  user: TransactionSigner<TAccountUser>;
  serverAccount?: Address<TAccountServerAccount>;
  systemProgram?: Address<TAccountSystemProgram>;
  serverId: ServerInitializeInstructionDataArgs["serverId"];
  serverType: ServerInitializeInstructionDataArgs["serverType"];
  allowedMerkleRoot: ServerInitializeInstructionDataArgs["allowedMerkleRoot"];
};

export async function getServerInitializeInstructionAsync<
  TAccountUser extends string,
  TAccountServerAccount extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof CODE_IN_PROGRAM_ADDRESS,
>(
  input: ServerInitializeAsyncInput<
    TAccountUser,
    TAccountServerAccount,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  ServerInitializeInstruction<
    TProgramAddress,
    TAccountUser,
    TAccountServerAccount,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? CODE_IN_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    user: { value: input.user ?? null, isWritable: true },
    serverAccount: { value: input.serverAccount ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.serverAccount.value) {
    accounts.serverAccount.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getUtf8Encoder().encode(expectSome(args.serverId)),
        getAddressEncoder().encode(expectAddress(accounts.user.value)),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.user),
      getAccountMeta(accounts.serverAccount),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getServerInitializeInstructionDataEncoder().encode(
      args as ServerInitializeInstructionDataArgs,
    ),
    programAddress,
  } as ServerInitializeInstruction<
    TProgramAddress,
    TAccountUser,
    TAccountServerAccount,
    TAccountSystemProgram
  >);
}

export type ServerInitializeInput<
  TAccountUser extends string = string,
  TAccountServerAccount extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  user: TransactionSigner<TAccountUser>;
  serverAccount: Address<TAccountServerAccount>;
  systemProgram?: Address<TAccountSystemProgram>;
  serverId: ServerInitializeInstructionDataArgs["serverId"];
  serverType: ServerInitializeInstructionDataArgs["serverType"];
  allowedMerkleRoot: ServerInitializeInstructionDataArgs["allowedMerkleRoot"];
};

export function getServerInitializeInstruction<
  TAccountUser extends string,
  TAccountServerAccount extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof CODE_IN_PROGRAM_ADDRESS,
>(
  input: ServerInitializeInput<
    TAccountUser,
    TAccountServerAccount,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): ServerInitializeInstruction<
  TProgramAddress,
  TAccountUser,
  TAccountServerAccount,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? CODE_IN_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    user: { value: input.user ?? null, isWritable: true },
    serverAccount: { value: input.serverAccount ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.user),
      getAccountMeta(accounts.serverAccount),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getServerInitializeInstructionDataEncoder().encode(
      args as ServerInitializeInstructionDataArgs,
    ),
    programAddress,
  } as ServerInitializeInstruction<
    TProgramAddress,
    TAccountUser,
    TAccountServerAccount,
    TAccountSystemProgram
  >);
}

export type ParsedServerInitializeInstruction<
  TProgram extends string = typeof CODE_IN_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    user: TAccountMetas[0];
    serverAccount: TAccountMetas[1];
    systemProgram: TAccountMetas[2];
  };
  data: ServerInitializeInstructionData;
};

export function parseServerInitializeInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedServerInitializeInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      user: getNextAccount(),
      serverAccount: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getServerInitializeInstructionDataDecoder().decode(instruction.data),
  };
}
