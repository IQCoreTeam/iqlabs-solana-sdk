/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getOptionDecoder,
  getOptionEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type Option,
  type OptionOrNullable,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { CODE_IN_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";
import {
  getSessionFinalizeDecoder,
  getSessionFinalizeEncoder,
  type SessionFinalize,
  type SessionFinalizeArgs,
} from "../types";

export const DB_CODE_IN_DISCRIMINATOR = new Uint8Array([
  38, 100, 165, 242, 99, 137, 206, 108,
]);

export function getDbCodeInDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(DB_CODE_IN_DISCRIMINATOR);
}

export type DbCodeInInstruction<
  TProgram extends string = typeof CODE_IN_PROGRAM_ADDRESS,
  TAccountUser extends string | AccountMeta<string> = string,
  TAccountDbAccount extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TAccountSession extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountUser extends string
        ? WritableSignerAccount<TAccountUser> & AccountSignerMeta<TAccountUser>
        : TAccountUser,
      TAccountDbAccount extends string
        ? WritableAccount<TAccountDbAccount>
        : TAccountDbAccount,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      TAccountSession extends string
        ? WritableAccount<TAccountSession>
        : TAccountSession,
      ...TRemainingAccounts,
    ]
  >;

export type DbCodeInInstructionData = {
  discriminator: ReadonlyUint8Array;
  onChainPath: string;
  metadata: string;
  session: Option<SessionFinalize>;
};

export type DbCodeInInstructionDataArgs = {
  onChainPath: string;
  metadata: string;
  session: OptionOrNullable<SessionFinalizeArgs>;
};

export function getDbCodeInInstructionDataEncoder(): Encoder<DbCodeInInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["onChainPath", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ["metadata", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ["session", getOptionEncoder(getSessionFinalizeEncoder())],
    ]),
    (value) => ({ ...value, discriminator: DB_CODE_IN_DISCRIMINATOR }),
  );
}

export function getDbCodeInInstructionDataDecoder(): Decoder<DbCodeInInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["onChainPath", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ["metadata", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ["session", getOptionDecoder(getSessionFinalizeDecoder())],
  ]);
}

export function getDbCodeInInstructionDataCodec(): Codec<
  DbCodeInInstructionDataArgs,
  DbCodeInInstructionData
> {
  return combineCodec(
    getDbCodeInInstructionDataEncoder(),
    getDbCodeInInstructionDataDecoder(),
  );
}

export type DbCodeInInput<
  TAccountUser extends string = string,
  TAccountDbAccount extends string = string,
  TAccountSystemProgram extends string = string,
  TAccountSession extends string = string,
> = {
  user: TransactionSigner<TAccountUser>;
  dbAccount: Address<TAccountDbAccount>;
  systemProgram?: Address<TAccountSystemProgram>;
  session?: Address<TAccountSession>;
  onChainPath: DbCodeInInstructionDataArgs["onChainPath"];
  metadata: DbCodeInInstructionDataArgs["metadata"];
  sessionArg: DbCodeInInstructionDataArgs["session"];
};

export function getDbCodeInInstruction<
  TAccountUser extends string,
  TAccountDbAccount extends string,
  TAccountSystemProgram extends string,
  TAccountSession extends string,
  TProgramAddress extends Address = typeof CODE_IN_PROGRAM_ADDRESS,
>(
  input: DbCodeInInput<
    TAccountUser,
    TAccountDbAccount,
    TAccountSystemProgram,
    TAccountSession
  >,
  config?: { programAddress?: TProgramAddress },
): DbCodeInInstruction<
  TProgramAddress,
  TAccountUser,
  TAccountDbAccount,
  TAccountSystemProgram,
  TAccountSession
> {
  // Program address.
  const programAddress = config?.programAddress ?? CODE_IN_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    user: { value: input.user ?? null, isWritable: true },
    dbAccount: { value: input.dbAccount ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
    session: { value: input.session ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input, session: input.sessionArg };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.user),
      getAccountMeta(accounts.dbAccount),
      getAccountMeta(accounts.systemProgram),
      getAccountMeta(accounts.session),
    ],
    data: getDbCodeInInstructionDataEncoder().encode(
      args as DbCodeInInstructionDataArgs,
    ),
    programAddress,
  } as DbCodeInInstruction<
    TProgramAddress,
    TAccountUser,
    TAccountDbAccount,
    TAccountSystemProgram,
    TAccountSession
  >);
}

export type ParsedDbCodeInInstruction<
  TProgram extends string = typeof CODE_IN_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    user: TAccountMetas[0];
    dbAccount: TAccountMetas[1];
    systemProgram: TAccountMetas[2];
    session?: TAccountMetas[3] | undefined;
  };
  data: DbCodeInInstructionData;
};

export function parseDbCodeInInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedDbCodeInInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  const getNextOptionalAccount = () => {
    const accountMeta = getNextAccount();
    return accountMeta.address === CODE_IN_PROGRAM_ADDRESS
      ? undefined
      : accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      user: getNextAccount(),
      dbAccount: getNextAccount(),
      systemProgram: getNextAccount(),
      session: getNextOptionalAccount(),
    },
    data: getDbCodeInInstructionDataDecoder().decode(instruction.data),
  };
}
