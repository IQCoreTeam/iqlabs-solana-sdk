/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  transformEncoder,
  type Account,
  type Address,
  type Codec,
  type Decoder,
  type EncodedAccount,
  type Encoder,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const DB_ROOT_DISCRIMINATOR = new Uint8Array([
  245, 92, 214, 180, 144, 59, 3, 240,
]);

export function getDbRootDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(DB_ROOT_DISCRIMINATOR);
}

export type DbRoot = {
  discriminator: ReadonlyUint8Array;
  creator: Address;
  tableSeeds: Array<ReadonlyUint8Array>;
  globalTableSeeds: Array<ReadonlyUint8Array>;
  id: ReadonlyUint8Array;
};

export type DbRootArgs = {
  creator: Address;
  tableSeeds: Array<ReadonlyUint8Array>;
  globalTableSeeds: Array<ReadonlyUint8Array>;
  id: ReadonlyUint8Array;
};

/** Gets the encoder for {@link DbRootArgs} account data. */
export function getDbRootEncoder(): Encoder<DbRootArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["creator", getAddressEncoder()],
      [
        "tableSeeds",
        getArrayEncoder(
          addEncoderSizePrefix(getBytesEncoder(), getU32Encoder()),
        ),
      ],
      [
        "globalTableSeeds",
        getArrayEncoder(
          addEncoderSizePrefix(getBytesEncoder(), getU32Encoder()),
        ),
      ],
      ["id", addEncoderSizePrefix(getBytesEncoder(), getU32Encoder())],
    ]),
    (value) => ({ ...value, discriminator: DB_ROOT_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link DbRoot} account data. */
export function getDbRootDecoder(): Decoder<DbRoot> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["creator", getAddressDecoder()],
    [
      "tableSeeds",
      getArrayDecoder(addDecoderSizePrefix(getBytesDecoder(), getU32Decoder())),
    ],
    [
      "globalTableSeeds",
      getArrayDecoder(addDecoderSizePrefix(getBytesDecoder(), getU32Decoder())),
    ],
    ["id", addDecoderSizePrefix(getBytesDecoder(), getU32Decoder())],
  ]);
}

/** Gets the codec for {@link DbRoot} account data. */
export function getDbRootCodec(): Codec<DbRootArgs, DbRoot> {
  return combineCodec(getDbRootEncoder(), getDbRootDecoder());
}

export function decodeDbRoot<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<DbRoot, TAddress>;
export function decodeDbRoot<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<DbRoot, TAddress>;
export function decodeDbRoot<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<DbRoot, TAddress> | MaybeAccount<DbRoot, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getDbRootDecoder(),
  );
}

export async function fetchDbRoot<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<DbRoot, TAddress>> {
  const maybeAccount = await fetchMaybeDbRoot(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeDbRoot<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<DbRoot, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeDbRoot(maybeAccount);
}

export async function fetchAllDbRoot(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<DbRoot>[]> {
  const maybeAccounts = await fetchAllMaybeDbRoot(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeDbRoot(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<DbRoot>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeDbRoot(maybeAccount));
}
