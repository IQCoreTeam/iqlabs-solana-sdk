/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  transformEncoder,
  type Account,
  type Address,
  type Codec,
  type Decoder,
  type EncodedAccount,
  type Encoder,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const TABLE_DISCRIMINATOR = new Uint8Array([
  34, 100, 138, 97, 236, 129, 230, 112,
]);

export function getTableDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(TABLE_DISCRIMINATOR);
}

export type Table = {
  discriminator: ReadonlyUint8Array;
  columnNames: Array<ReadonlyUint8Array>;
  idCol: ReadonlyUint8Array;
  extKeys: Array<ReadonlyUint8Array>;
  name: ReadonlyUint8Array;
  lastTimestamp: bigint;
  gateMint: Address;
  writers: Array<Address>;
};

export type TableArgs = {
  columnNames: Array<ReadonlyUint8Array>;
  idCol: ReadonlyUint8Array;
  extKeys: Array<ReadonlyUint8Array>;
  name: ReadonlyUint8Array;
  lastTimestamp: number | bigint;
  gateMint: Address;
  writers: Array<Address>;
};

/** Gets the encoder for {@link TableArgs} account data. */
export function getTableEncoder(): Encoder<TableArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      [
        "columnNames",
        getArrayEncoder(
          addEncoderSizePrefix(getBytesEncoder(), getU32Encoder()),
        ),
      ],
      ["idCol", addEncoderSizePrefix(getBytesEncoder(), getU32Encoder())],
      [
        "extKeys",
        getArrayEncoder(
          addEncoderSizePrefix(getBytesEncoder(), getU32Encoder()),
        ),
      ],
      ["name", addEncoderSizePrefix(getBytesEncoder(), getU32Encoder())],
      ["lastTimestamp", getI64Encoder()],
      ["gateMint", getAddressEncoder()],
      ["writers", getArrayEncoder(getAddressEncoder())],
    ]),
    (value) => ({ ...value, discriminator: TABLE_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link Table} account data. */
export function getTableDecoder(): Decoder<Table> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    [
      "columnNames",
      getArrayDecoder(addDecoderSizePrefix(getBytesDecoder(), getU32Decoder())),
    ],
    ["idCol", addDecoderSizePrefix(getBytesDecoder(), getU32Decoder())],
    [
      "extKeys",
      getArrayDecoder(addDecoderSizePrefix(getBytesDecoder(), getU32Decoder())),
    ],
    ["name", addDecoderSizePrefix(getBytesDecoder(), getU32Decoder())],
    ["lastTimestamp", getI64Decoder()],
    ["gateMint", getAddressDecoder()],
    ["writers", getArrayDecoder(getAddressDecoder())],
  ]);
}

/** Gets the codec for {@link Table} account data. */
export function getTableCodec(): Codec<TableArgs, Table> {
  return combineCodec(getTableEncoder(), getTableDecoder());
}

export function decodeTable<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<Table, TAddress>;
export function decodeTable<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<Table, TAddress>;
export function decodeTable<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<Table, TAddress> | MaybeAccount<Table, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getTableDecoder(),
  );
}

export async function fetchTable<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<Table, TAddress>> {
  const maybeAccount = await fetchMaybeTable(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeTable<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<Table, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeTable(maybeAccount);
}

export async function fetchAllTable(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<Table>[]> {
  const maybeAccounts = await fetchAllMaybeTable(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeTable(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<Table>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeTable(maybeAccount));
}
